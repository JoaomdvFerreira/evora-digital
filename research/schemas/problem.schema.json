{
  "prefix": "PRB-",
  "directory": "problems",
  "idField": "problem_id",
  "sourceModel": "docs/models/problem-model.md",
  "notes": "domain accepts either a single string or a list of domain codes; docs/models/problem-model.md shows a single string, but multi-domain problems (see WU-D1-02) are recorded as a list, consistent with the existing evidence.domain list convention. No enum is enforced on domain values.",
  "requiredFields": [
    "problem_id",
    "title",
    "domain",
    "geography",
    "geography.level",
    "affected_populations",
    "problem_statement",
    "evidence",
    "evidence_status",
    "validation_status",
    "digital_tractability",
    "existing_solutions",
    "status"
  ],
  "enums": {
    "evidence_status": ["discovered", "corroborated"],
    "validation_status": ["unvalidated", "partially_validated", "validated"],
    "digital_tractability": ["not_assessed", "low", "medium", "high"],
    "existing_solutions": ["not_assessed", "assessed"],
    "status": ["OPEN", "REJECTED", "DUPLICATE", "NON_DIGITAL", "ALREADY_SOLVED", "INSUFFICIENT_EVIDENCE"]
  },
  "references": [
    {
      "field": "evidence",
      "isList": true,
      "targetPrefix": "EVD-",
      "targetDirectory": "evidence",
      "required": false
    }
  ]
}
